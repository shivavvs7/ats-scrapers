services:
  - type: web
    name: jobhive-board
    runtime: python
    buildCommand: pip install -e . && pip install fastapi uvicorn[standard]
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free